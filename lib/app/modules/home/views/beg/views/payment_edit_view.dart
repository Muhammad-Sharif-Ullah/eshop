import 'package:flutter/material.dart';

class PaymentEditView extends StatelessWidget {
  const PaymentEditView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Payment Eidit View')),
    );
  }
}
